# Production canonical domain: salim-prevention-desinfection.tn
# (OVH order 259071503). DNS cutover to Cloudflare still pending —
# site is reachable via Workers URL until nameservers are switched
# and the custom domain is attached to the Worker.
NEXT_PUBLIC_SITE_URL=https://salim-prevention-desinfection.tn
